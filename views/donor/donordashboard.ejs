<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="icon" href="/images/favicon.ico">
<!-- Bootstrap CSS -->
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
<link rel="stylesheet" href="/pages/donordashboard.css">
<title><%= title %></title>
</head>
<body>

    <nav class="nav nav_top">
        <div class="logo">
            <a href="/"><img src="/images/logo.png" width="250"></a>
        </div>
        <div class="nav_side">
            <a href="/">HOME</a>
            <a href="/about">ABOUT US</a>
            <a href="/donor/donations"> MY LIST</a>
            <a href="/donor/logout">LOGOUT</a>
        </div>
    </nav>

    <blockquote class="blockquote text-center">
        <h1>Hostel List</h1>
    </blockquote>
    
    <!-- <table class="table">
        <thead class="thead-dark">
          <tr>
            <th scope="col">S.No.</th>
            <th scope="col">Name</th>
            <th scope="col">Location</th>
            <th scope="col">Price</th>
            <th scope="col">Details</th>
            <th scope="col">Image</th>
            <th scope="col">Book</th>
          </tr>
        </thead>
        <tbody>
            <%
            if(martyrData.length!=0){
            var i=1;
            martyrData.forEach(function(data){
            %>
          <tr>
            <th scope="row"><%=i; %></th>
            <td><%=data.name %></td>
            <td><%=data.location %></td>
            <td><%=data.price %></td>
            <td><%=data.info %></td>
            <td><%=data.image %></td>
            <td><a href="/payment/<%=data.id%>" class="btn btn-success">Book</a></td>
          </tr>
          <%  i++; }) %>
          <% } else{ %>
              <tr>
                  <td colspan="7">No Data Found</td>
              </tr>
          <% } %>
        </tbody>
      </table> -->

      <div class="container mx-auto mt-4">
        <div class="row">
          <% if(martyrData.length!=0){ var i=1; martyrData.forEach(function(data){
          %>
          <div class="col-md-4">
            <div class="card" style="width: 18rem">
              <img
                src="/images/hostel-dorm-bedroom-two.jpg"
                class="card-img-top"
                alt="..."
              />
              <div class="card-body">
                <h5 class="card-title"><%=data.name %></h5>
                <h6 class="card-subtitle mb-2 text-muted">Location :</b> <%=data.location %></h6>
                <p class="txt2"><b>Price :</b><%=data.price %></p>
                <p class="card-text">
                  <%=data.info %>
                </p>
                
                <a href="/payment/<%=data.id%>" class="button_green">Book</a>
                
              </div>
            </div>
          </div>
          <% i++; }) %> <% } else{ %>
            <p>No Data Found</p>
          <% } %>
        </div>
      </div>
</body>
</html>

